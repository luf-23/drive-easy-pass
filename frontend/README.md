# drive easy pass 前端项目仓库

## 安装依赖

1. 安装 Node.js 和 npm（Node 包管理器）。可以从 [Node.js 官网](https://nodejs.org/) 下载并安装最新版本。

2. 在项目根目录下运行以下命令安装项目依赖：

```bash
cd ./frontend
npm install
```

## 运行项目

在项目根目录下运行以下命令启动开发服务器：

```bash
npm run dev
```
